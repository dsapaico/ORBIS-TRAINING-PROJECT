'use strict';

var bulbo = require('../../');
var chalk = require('chalk');

/**
 * The serve action.
 */
module.exports = function () {
  bulbo.cli.liftoff('bulbo').then(function (_ref) {
    var bulbo = _ref.module;

    if (bulbo.isEmpty()) {
      bulbo.logger.log(chalk.red('Error: No asset defined'));

      process.exit(1);
    }

    bulbo.serve();
  });
};